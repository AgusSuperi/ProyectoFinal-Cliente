import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  root: {
    top: "50%",
    left: "30%",
    position: "absolute",
    width: "200px",
    height: "100px",
  },
}));
